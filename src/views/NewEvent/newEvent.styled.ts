import styled from 'styled-components';

export const EventForm = styled.form`
  display: flex;
  flex-direction: column;
`;

export const Wrapper = styled.div`
  padding: 2rem;
  width: 482px;
  position: relative;
  z-index: 1;
  margin: 1rem;
  height: 726px;
  background-color: #FFF;
  box-shadow: 0 0 1px 0 rgba(0, 0, 0, 0.30), 0 2px 30px 0 rgba(0, 0, 0, 0.08), 0 0 15px 0 rgba(0, 0, 0, 0.03);
  border-radius: 25px;
  display: flex;
  flex-direction: column;
  gap:2rem
`;
