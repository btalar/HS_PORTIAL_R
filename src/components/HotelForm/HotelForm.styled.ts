import styled from 'styled-components';

export const HotelFormWrapper = styled.form`
    display: flex;
    flex-direction: column;
    gap:1rem;
    background-color: white;
    padding: 1rem;
    border-radius: 1rem;
`;
